# WhatsApp authentication data (generated by Baileys)
auth_info

# Configuration file with runtime settings (use examples/config.example.json)
config.json

# Build artifacts generated by tsc (compiled JS files)
dist

# Node.js dependencies (regenerate with npm install)
node_modules

# User data file with sensitive API keys (use examples/users.example.json)
users.json
